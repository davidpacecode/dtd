<p style="color: green"><%= notice %></p>

<% content_for :title, "Tasks" %>

<h2><%= "#{@view.titleize} Tasks" %></h2>

<div id="tasks">

  <%= turbo_frame_tag "task_table" do %>
    <%= render "task_table", tasks: @tasks, view: @view %>
  <% end %>

</div>

<%= link_to "New task", new_task_path %>
